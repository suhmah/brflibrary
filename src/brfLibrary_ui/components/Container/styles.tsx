import Metrics from '@/brfLibrary_ui/helpers/metric';
import styled from 'styled-components/native';

export const Main = styled.SafeAreaView`
  flex: 1;
`;
export const Wrapper = styled.View`
  padding: ${Metrics(16)}px 0;
`;
