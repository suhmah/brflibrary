import Metrics from './helpers/metric';

const fontSizes = {
  title: Metrics(24),
  title_h2: Metrics(16),
  subtitle: Metrics(12),
  body_1: Metrics(16),
  body_2: Metrics(14),
  description: Metrics(10),
};

export default fontSizes;
