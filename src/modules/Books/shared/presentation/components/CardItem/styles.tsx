import { metric } from '@/brfLibrary_ui';
import styled from 'styled-components/native';

export const Main = styled.View`
  width: 100%;

  flex-direction: row;
`;

export const WrapperImage = styled.Image`
  width: ${metric(81)}px;
  height: ${metric(125)}px;
`;
